"""Color palette and Plotly layout defaults for the dashboard.

Mirrors the ``.streamlit/config.toml`` theme so charts and chrome stay visually
consistent.

Implemented in step 5 of the build sequence.
"""

from __future__ import annotations
